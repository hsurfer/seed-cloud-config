spring:
  application:
    name: apps-cloud-config
  cloud:
    config:
      env: develop
      server:
        bootstrap: true
        prefix: /config
        git:
          uri: file:/home/gustavo/projects/POCS/base-cloud-configs
          username: gdmuzzillo
          password: Aragorn37365#
encrypt:
  key: encryptme
security:
  user:
    password: sarasa
